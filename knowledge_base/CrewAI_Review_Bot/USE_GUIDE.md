# Knowledge Base: CrewAI_Review_Bot

> Auto-generated by **Project Understanding System** on 2026-05-06

## Structure

```
CrewAI_Review_Bot/
├── snapshots/           # Historical snapshots by date
│   └── YYYY-MM-DD/
│       └── snapshot.json
├── indexes/             # Semantic search indexes
│   └── semantic_index.json
├── latest.json          # Current snapshot (copy of newest)
├── metadata.json        # Project metadata & stats
└── USE_GUIDE.md         # This file
```

## Statistics

| Metric | Count |
|--------|-------|
| Files | 17 |
| Symbols | 17 |
| Relations | 52 |
| Summaries | 34 |

**Last commit:** `ec130692e8ab` on branch `main`

**Top file types:** `.py` (10), `.bat` (2), `.txt` (2), `.example` (1), `.gitignore` (1)

**Top symbol kinds:** `constant` (7), `method` (7), `function` (2), `class` (1)

## How to Update

```bash
# Basic ingestion (heuristic summaries)
python -m project_understanding.cli.main ingest /path/to/CrewAI_Review_Bot --project-name CrewAI_Review_Bot

# With LLM summaries (recommended)
python -m project_understanding.cli.main ingest /path/to/CrewAI_Review_Bot --project-name CrewAI_Review_Bot

# Skip enrichment for faster results
python -m project_understanding.cli.main ingest /path/to/CrewAI_Review_Bot --project-name CrewAI_Review_Bot --no-enrichment
```

## Query Examples

```bash
# Get context for a specific file
python -m project_understanding.cli.main query file "src/api/auth.ts" --repo CrewAI_Review_Bot --profile review-agent

# Search for a symbol
python -m project_understanding.cli.main query symbol "LoginRequest" --repo CrewAI_Review_Bot --profile dev-agent

# Semantic search
python -m project_understanding.cli.main query semantic "authentication flow" --repo CrewAI_Review_Bot --profile review-agent

# Change impact analysis
python -m project_understanding.cli.main query changes "src/api/auth.ts" "src/utils/token.ts" --repo CrewAI_Review_Bot --profile review-agent
```

## Agent Profiles

| Profile | Use Case |
|---------|----------|
| `review-agent` | Code review, PR analysis |
| `dev-agent` | Development assistance, debugging |
| `doc-agent` | Documentation generation |

## Output Format

Each query returns a **ContextBundle** containing:
- **Items**: Files, symbols, modules relevant to the query
- **Relations**: Dependencies and call graphs
- **Summaries**: LLM-generated or heuristic descriptions
- **Conventions**: Detected coding patterns
- **Risks**: Identified risk areas

---

*This file was auto-generated. Edit freely — it won't be overwritten on re-ingest.*
